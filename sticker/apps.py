''' App for generate sticker. We glue the sticker when we take the device to repair.'''
from django.apps import AppConfig


class StickerConfig(AppConfig):
    ''' Default app'''
    name = 'sticker'
    label = 'sticer'
